# secrets and local data
.env
# the copies the installer keeps when it replaces .env hold the same keys
.env.*
!.env.example
data/
caddy/
backups/
*.db
*.sqlite
*.sqlite3
token.json

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.ruff_cache/
.pytest_cache/
pytest-of-*/

# node (web UI later)
node_modules/
