# secrets and local data
.env
# the copies the installer keeps when it replaces .env hold the same keys
.env.*
!.env.example
data/
caddy/
backups/
*.db
*.sqlite
*.sqlite3
token.json

# agents' working copies of the repo
.claude/worktrees/

# python
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.ruff_cache/
.pytest_cache/
pytest-of-*/

# uv's cache and the Python it fetched, which the installer keeps inside the checkout
/.cache/
/.local/
